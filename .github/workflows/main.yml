name: Unzip File
on: [push]
jobs:
  unzip:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: unzip yourfile.zip && rm yourfile.zip
      - run: git add . && git commit -m "Unzipped files" && git push
